<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <title>Vendors – Free Intel Hub</title>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body>

<%- include("partials/navbar") %>

<div class="container">
  <div class="section-title">Vendors</div>

  <div class="grid">
    <% vendors.forEach(v => { %>
      <a class="card" href="/vendor/<%= v.vendor %>">
        <h4><%= v.vendor %></h4>
        <div class="meta"><%= v.count %> articles</div>
      </a>
    <% }) %>
  </div>
</div>

<%- include("partials/footer") %>
